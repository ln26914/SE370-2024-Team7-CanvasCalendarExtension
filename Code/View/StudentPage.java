import javax.swing

public class StudentPage implements ExtensionTab {
    // Special ExtensionTab
    // Display a user's to-do list, name, and profile picture 
    // also, show buttons to navigate to other components of the extension
}